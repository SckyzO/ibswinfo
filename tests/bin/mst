#!/usr/bin/env bash

# Mock pour mst
if [[ "$1" == "version" ]]; then
    echo "mst version 4.28.0, built on Dec 14 2023"
    exit 0
fi

if [[ "$1" == "status" ]]; then
    echo "MST modules:"
    echo "------------"
    echo "    MST PCI module loaded"
    echo "    MST PCI configuration module loaded"
    echo ""
    echo "MST devices:"
    echo "------------"
    echo "/dev/mst/SW_MT53131_Quantum_lid-0x0001"
    exit 0
fi
